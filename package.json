{
  "name": "mongoosefindarray",
  "version": "1.0.0",
  "description": "Find all mongodb items in an Array.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist"
  },
  "author": "Sam Pettersson <sam@sampettersson.com>",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.4.6",
    "mongoose": "^4.6.0"
  },
  "devDependencies": {
    "babel-cli": "^6.14.0",
    "babel-plugin-transform-class-properties": "^6.11.5",
    "babel-preset-es2015": "^6.14.0"
  }
}
